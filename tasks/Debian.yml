---
  # Needed due to incapability of ansible to update the cache with the apt command
- name: Update apt cache manually
  command: apt-get update

- name: Install the required packages in Debian derivatives
  apt: name=ntp
